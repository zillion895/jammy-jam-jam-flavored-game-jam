[gd_scene format=3 uid="uid://m3ebc0xgd1aq"]

[sub_resource type="GDScript" id="GDScript_bcx1r"]
script/source = "extends CharacterBody3D


const SPEED = 5.0
const JUMP_VELOCITY = 4.5


func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var input_dir := Input.get_vector(\"ui_left\", \"ui_right\", \"ui_up\", \"ui_down\")
	var direction := (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()
	if direction:
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		velocity.z = move_toward(velocity.z, 0, SPEED)

	move_and_slide()
"

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_7uws0"]
height = 1.418457

[node name="player" type="CharacterBody3D" unique_id=1842708771]
script = SubResource("GDScript_bcx1r")

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=981220878]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.29077148, 0)
shape = SubResource("CapsuleShape3D_7uws0")

[node name="orientation" type="Node3D" parent="." unique_id=418270605]

[node name="art goes here remove this when redundant" type="Node" parent="orientation" unique_id=953085044]
